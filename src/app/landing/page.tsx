import { LandingPage } from '@/components/LandingPage'

// Always renders the neural-eye landing, regardless of auth — this is where the
// Fixate logo links to.
export default function Landing() {
  return <LandingPage />
}
